{
  "buildCommand": "cd learnova-web && npm install && npx prisma generate && next build",
  "outputDirectory": "learnova-web/.next",
  "installCommand": "cd learnova-web && npm install",
  "framework": "nextjs"
}